<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
  <%- include('../partials/header'); %>
</header>

<main>
	<div class="card mt-2">
		
		<div class="card-header">
	     <h5 class="card-title">BORSA TREBALL</h5>
	  </div>

    <div class="card-body">
	    
			<table class="table table-striped">
		  		<thead>
				    <tr>		      
				      <th scope="col">Titol</th>
					  <th scope="col">Descripcio</th>
					  <th scope="col">Skills</th>
					  <th scope="col">Empresa</th>
				     
				      </th>
				    </tr>
		  		</thead>
			  	<tbody>
			  		<% listaOfertas.forEach( function(oferta) { %>
					    <tr>
							      
					      <td><%= oferta.title %></td>
						  <td><%= oferta.description %></td>
						  <td><%= oferta.skills %></td>
						  <td><%= oferta.createBy %></td>
						  <td>
							<a href="update/<%= oferta._id %>"><button type="button" class="btn btn-primary">Update</button></a>
							<a href="remove/<%= oferta._id %>" ><button type="button" class="btn btn-danger">Delete</button></a>
					      </td>
					      </td>		      
					    </tr>
			   		<% }); %>	     
			    </tbody>
		  </table>	     
	    
		</div>
		
	</div>
 
</main>

<footer>
  <%- include('../partials/footer'); %>
</footer>

</body>
</html>